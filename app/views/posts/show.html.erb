<h1><%= @post.title %></h1>
<small><%= @post.created_at.strftime("%b %d, %Y") %></small>
<p><%= @post.body %></p>
<#%= link_to "Edit Post",edit_post_path %> 
<#%= link_to "Delete", @post, data: { confirm: "Are you sure you want to delete?"},
	:method => :delete %>
<br />
<br />
<p>
	<%= link_to @post.category.name,category_path(:id => @post.category.id) %>
</p>

<%= @post.category.name %>
<%= link_to "Go Back",posts_path %>